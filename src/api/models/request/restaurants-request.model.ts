export interface CreateRestaurantRequest {
    name: string;
    phoneNumber: string;
    address: string;
}

export interface ChangeRestaurantRequest {
    name: string;
    phoneNumber: string;
    address: string;
}